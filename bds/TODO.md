resolver: function which maps labels / function names to code points (not merely blocks)

idea: 'longshot' format: convert everything into something much closer to an ast, with e.g. working symbol references, a global symbol table, etc. 
