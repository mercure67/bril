extract = 'total_dyn_inst: (\d+)'
benchmarks = '../benchmarks/*/*.bril'

[runs.baseline]
pipeline = [
    "bril2json",
    "brili -p {args}",
]

[runs.lvn]
pipeline = [
    "bril2json",
    "target/debug/rust_stuff --mode pipe - dce",
    "bril2json",
    "brili -p {args}",
]
